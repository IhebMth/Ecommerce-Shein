# ============================================================
# NOVA Store — .env.example
# Copy this file to .env and fill in your real values.
# NEVER commit .env to GitHub — only commit this file.
# ============================================================

# ── SUPABASE ─────────────────────────────────────────────────
# Your Supabase project URL
# Found at: supabase.com → your project → Settings → API → Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase secret (service role) key — gives full DB access
# Found at: supabase.com → your project → Settings → API → secret key
SUPABASE_SERVICE_KEY=your-supabase-secret-key-here

# ── UPSTASH REDIS (rate limiting) ────────────────────────────
# Your Upstash Redis REST URL
# Found at: upstash.com → your database → REST API section
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io

# Your Upstash Redis REST token
# Found at: upstash.com → your database → REST API section
UPSTASH_REDIS_REST_TOKEN=your-upstash-token-here

# ── RESEND (order notification emails) ───────────────────────
# Your Resend API key — shown ONCE at creation
# Found at: resend.com → API Keys
RESEND_API_KEY=re_your_resend_api_key_here

# The email address where you want to receive order notifications
RESEND_TO_EMAIL=your@email.com

# ── SECURITY SECRETS ─────────────────────────────────────────
# Secret used for admin authentication — 32+ random characters
# You invent this — keep it private
ADMIN_SECRET=your-32-plus-char-random-string-here

# Secret used to sign JWT tokens — 32+ random characters
# Must be DIFFERENT from ADMIN_SECRET — keep it private
JWT_SECRET=another-different-32-plus-char-string-here

#Simple way to remember:
#You are setting this on the backend ✅
#The value is the frontend URL ✅
#It means: "my API only accepts requests from my store" ✅
ALLOWED_ORIGIN=https://your-nova-store.vercel.app

# ── ADMIN USER (for initial setup) ─────────────────────────── 
# The email for your initial admin user
# This is the email you'll use to log in to the admin dashboard.
node database/create-admin.js your@email.com YourPassword= this will create an admin user with the provided email and password.
  